import { FC } from "react";
import { StyleHeaderText, StyleHeaderContainer } from "../styles/styles";

const HeaderContainer: FC = () => {
  return (
    <div>
      <StyleHeaderContainer>
        <StyleHeaderText>CRITICAL HITS - EFFECTS GENERATOR</StyleHeaderText>
      </StyleHeaderContainer>
    </div>
  );
};

export default HeaderContainer;
